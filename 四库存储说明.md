# 项目四库存储说明（MongoDB / Redis / Neo4j / MinIO）

本文档说明当前项目中 **MongoDB、Redis、Neo4j、MinIO** 四个存储分别存放哪些业务数据，便于理解整体数据分布。

---

## 一、总览

| 存储    | 类型       | 主要用途                     |
|---------|------------|------------------------------|
| **MongoDB** | 文档数据库 | 业务配置、任务、用户、元数据、IoT 连接配置等 |
| **Redis**   | 内存键值   | 验证码、任务队列与任务缓存   |
| **Neo4j**   | 图数据库   | 场景图、实例树、图表项目等关系与属性 |
| **MinIO**   | 对象存储   | 模型文件、瓦片、预览图、附件等二进制文件 |

---

## 二、MongoDB 存储内容

MongoDB 使用 **集合（Collection）** 存储文档，当前涉及的主要集合如下。

### 2.1 用户与认证

| 集合       | 说明 |
|------------|------|
| **users** | 用户账号信息（用户名、手机号、密码哈希等），用于登录与权限 |

### 2.2 数据源 / IoT 连接配置（数据管理）

| 集合                 | 说明 |
|----------------------|------|
| **mqtt_sources**     | MQTT 连接配置（Broker 地址、端口、认证、主题等），对应「数据管理 → MQTT」 |
| **websocket_sources**| WebSocket 数据源配置 |
| **http_sources**     | HTTP 数据源配置 |
| **mqtt_connections** | （若与 mqtt_sources 并存，可能为另一套 MQTT 连接抽象，供 IoT 绑定使用） |
| **http_connections** | HTTP 连接配置（供 IoT 绑定使用） |
| **websocket_connections** | WebSocket 连接配置（供 IoT 绑定使用） |

### 2.3 任务与文件

| 集合       | 说明 |
|------------|------|
| **tasks**  | 异步任务（如模型转换、3DTiles 处理、WMTS 处理等）的任务记录与状态 |
| **files**  | 文件元数据（与 MinIO 中的文件对应，如 file_id、路径、状态等），任务会引用 file_id |

### 2.4 模型与资产元数据

| 集合                    | 说明 |
|-------------------------|------|
| **metadata**            | 模型/产品的元数据（如解析后的结构、部件信息等），通过 file_id 关联 files |
| **public_models**       | 公共模型库的元数据（分类、标签、下载量、推荐等），文件本体在 MinIO |
| **threedtiles**         | 3DTiles 资产元数据（名称、描述、tileset 信息等） |
| **threedtiles_process_status** | 3DTiles 处理过程状态 |
| **gaussian_splats**     | 高斯泼溅（Gaussian Splatting）资产元数据 |

### 2.5 瓦片与 GIS

| 集合                  | 说明 |
|-----------------------|------|
| **wmts_layers**       | WMTS 瓦片图层配置与元数据 |
| **wmts_process_status** | WMTS 处理状态 |

### 2.6 其他业务

| 集合                  | 说明 |
|-----------------------|------|
| **streams**           | 流媒体/推流相关配置（按用户隔离） |
| **attachments**       | 附件元数据（名称、类型、关联等），文件本体在 MinIO |
| **instance_properties** | （与场景实例相关的部分属性缓存或扩展，可能与 Neo4j Instance 配合使用） |

**小结**：MongoDB 主要存 **配置、任务、用户、各类资产与数据源的元数据**，以及部分 **处理状态**；**实际大文件在 MinIO**，**场景与实例的层级与属性在 Neo4j**。

---

## 三、Redis 存储内容

Redis 用于 **临时、高频、带过期** 的数据，当前主要用途如下。

### 3.1 验证码

| 键模式 / 用途 | 说明 |
|----------------|------|
| `verification_code:{phone}` | 手机号对应的验证码，带过期时间（用于登录/注册验证） |

### 3.2 任务系统

| 用途       | 说明 |
|------------|------|
| **任务详情缓存** | 以任务 ID 为键，缓存任务状态与结果，减少对 MongoDB 的查询，并设过期时间 |
| **任务队列**    | 使用 List 结构（如 `rpush`）维护待执行任务队列，供异步 Worker 消费 |

**小结**：Redis 存 **验证码** 和 **任务队列 + 任务缓存**，特点是 **短期、可过期、高并发读写**。

---

## 四、Neo4j 存储内容

Neo4j 用 **节点（Node）+ 关系（Relationship）** 表示图结构，当前主要存 **场景图** 和 **图表项目**。

### 4.1 场景图（Scene Graph）

| 节点类型   | 说明 |
|------------|------|
| **Scene**  | 场景：名称、所有者、预览图链接（MinIO URL）、场景原点（origin）、绑定的图表 ID（chart_binds）、瓦片绑定（tiles_binding）等 |
| **Instance**| 场景中的实例节点：名称、资产 ID（asset_id）、资产类型（asset_type）、**transform**（location/rotation/scale）、**properties**、**iot_binds**（IoT 绑定配置）、video_binds、file_binds 等 |

**关系**：

- `Scene -[:ROOT]-> Instance`：场景根实例
- `Instance -[:PARENT_OF]-> Instance`：实例树父子关系（可嵌套）
- `Scene -[:SCENE_PARENT_OF]-> Scene`：场景层级（若有）
- `Scene -[:HAS_INSTANCE]-> Instance`：场景包含的实例

**要点**：

- **场景结构、实例树、每个实例的 transform / properties / iot_binds** 都在 Neo4j。
- 场景预览图、模型文件等 **大文件路径** 可能指向 MinIO；**资产 ID** 会与 MongoDB/MinIO 的资产元数据关联。

### 4.2 图表（GoView）

| 节点类型 | 说明 |
|----------|------|
| **Chart** | GoView 图表项目：名称、描述、所有者、完整配置（config 等） |

**小结**：Neo4j 存 **场景与实例的图结构及属性**、**图表的配置与归属**，适合表达「场景-实例」层级和关系。

---

## 五、MinIO 存储内容

MinIO 是 **对象存储（S3 兼容）**，按 **桶（Bucket）** 存放 **二进制文件**。

### 5.1 当前使用的桶（参考 `minio_client.py`）

| 桶名 | 说明 |
|------|------|
| **sourece-files**   | 源文件（如上传的原始模型、待转换文件） |
| **converted-files** | 转换后的文件（如转换后的模型） |
| **attachments**     | 附件文件 |
| **preview**         | 预览图（如场景预览图），可设为公开读 |
| **public-models**   | 公共模型文件（如 .glb），可设为公开读 |
| **threedtiles**     | 3DTiles 瓦片数据（目录/文件） |
| **gaussian-splats** | 高斯泼溅数据文件 |
| **chart-previews**  | 图表预览图 |
| **goview-files**    | GoView 相关文件 |

### 5.2 与其他库的配合

- **MongoDB**：存文件的 **元数据、路径、object_id** 等；任务、资产表通过 file_id / path 等字段指向 MinIO。
- **Neo4j**：Scene 的 `preview_image` 等属性存的是 MinIO 的 URL 或路径；Instance 的 `asset_id` 关联到资产，资产文件在 MinIO。

**小结**：MinIO 存 **所有需要持久化的大文件**（模型、瓦片、预览图、附件等），MongoDB/Neo4j 只存 **元数据与引用**。

---

## 六、四库分工小结（便于分享）

- **MongoDB**：业务配置与元数据（用户、IoT 连接、任务、资产元数据、处理状态等）。
- **Redis**：临时与高并发数据（验证码、任务队列、任务缓存）。
- **Neo4j**：图结构数据（场景图、实例树、实例属性与 IoT 绑定、图表项目）。
- **MinIO**：所有大文件（模型、瓦片、预览图、附件等）。

**简单记**：  
**MongoDB = 配置与元数据，Redis = 缓存与队列，Neo4j = 场景与图表关系，MinIO = 文件。**

---

## 七、按业务场景看数据落在哪

| 业务           | MongoDB | Redis | Neo4j | MinIO |
|----------------|---------|-------|-------|-------|
| 用户登录       | users   | 验证码 | -     | -     |
| MQTT/HTTP/WS 配置 | mqtt_sources 等 | - | - | - |
| 场景与实例树   | -       | -     | Scene, Instance | 预览图等 |
| 实例 transform/iot_binds | - | - | Instance 属性 | - |
| 模型/3DTiles/高斯 元数据 | public_models, threedtiles, gaussian_splats | - | - | 对应桶中文件 |
| 异步任务       | tasks, files | 任务队列/缓存 | - | 任务涉及的文件 |
| 图表项目       | -       | -     | Chart | goview-files, chart-previews |
| WMTS 瓦片      | wmts_layers 等 | - | - | 瓦片文件（若用 MinIO） |

---

**文档生成时间**：2026-02-06  
若后续新增集合或桶，可在本文档中按相同格式补充。
