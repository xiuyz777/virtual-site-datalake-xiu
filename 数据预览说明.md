# 数据预览说明

本文说明「数据预览」弹窗（数据预览 - MQTT/HTTP/WebSocket）的入口、主要逻辑与各协议下的数据拉取方式。

---

## 一、概述

- **作用**：在配置 IoT 绑定时，用户需要指定「数据源路径」（即从 MQTT/HTTP/WebSocket 的哪一字段取值）。数据预览弹窗会**按当前选中的协议和连接，拉取或收集一份真实数据**，展成树形结构，用户点击节点即可选中路径并回填到「数据源路径」输入框。
- **位置**：`web/src/components/DataPathHelper.tsx` 中的 **DataPreviewModal** 组件。
- **核心函数**：**fetchDataPreview**（约 691–839 行），负责按协议拉取/组装预览数据并调用 `setPreviewData(data)`；左侧「数据结构」树由 **buildDataTree(previewData)** 渲染。

---

## 二、入口

- **触发位置**：`web/src/components/IoTBindingConfigModal.tsx`，在绑定映射配置里「数据源路径」旁的**云图标按钮**（`title="预览数据"`）。
- **前置条件**：已选择**协议**（MQTT/HTTP/WebSocket）和**数据源**（即某条连接的 `id`，作为 `connectionId`）。
- **打开弹窗**：点击后 `setShowDataPreview(true)`，渲染 **DataPreviewModal**，并传入 `visible`、`protocol`、`connectionId`、`onClose`、`onPathSelect`。
- **弹窗打开时**：`useEffect` 在 `visible === true` 时清空 `previewData`、`selectedPath`、`expandedKeys`，并**自动调用一次 fetchDataPreview()**（约 1028–1035 行）。

---

## 三、fetchDataPreview 主流程

```
setLoading(true)
  → 若非 MQTT：清空 collectedMessages、connectionStatus
  → 按 protocol 分支：
      - HTTP：拉取 HTTP 配置 → fetch(base_url) → 解析 JSON/文本 → data
      - MQTT：拉取/使用 MQTT 配置 → 等待 8 秒收集消息 → 从 ref 取消息 → 拼 _config + 消息或 debug_info → data
      - WebSocket：当前为模拟数据 → data
  → setPreviewData(data)
  → message.success / catch 里 message.error
  → finally: setLoading(false)
```

- **状态与 ref**：  
  - `previewData`：最终展示用的数据，驱动左侧树。  
  - MQTT 专用：`mqttConfig`、`collectedMessages`、`connectionStatus`；**collectedMessagesRef** 存实际收到的消息（避免 async 闭包拿到旧 state）。

---

## 四、各协议下的数据拉取逻辑

### 4.1 HTTP（约 702–724 行）

1. 使用 **connectionId** 调用 `httpAPI.getHTTPById(connectionId)` 获取该 HTTP 连接的配置（base_url、method、headers 等）。
2. 使用 **fetch** 按配置请求：`method` 默认 GET，headers 包含 `Content-Type: application/json` 与配置中的自定义 headers。
3. 若 `response.ok`：  
   - 若 `Content-Type` 包含 `application/json`，则 `data = await response.json()`。  
   - 否则 `data = { text: await response.text() }`，保证树至少有一个节点。
4. 若不 ok：`throw new Error(...)`，由外层 catch 统一提示「获取数据预览失败」。

### 4.2 MQTT（约 725–807 行）

MQTT 是长连接、消息异步到达，因此不是「发一次请求拿一次响应」，而是：**先确保有配置与连接、已订阅主题，再等一段时间，把这段时间内收到的消息作为预览数据。**

1. **获取配置**  
   - 若已有 `mqttConfig` 则直接用。  
   - 否则 `mqttAPI.getMQTTById(connectionId)` 取得配置，并延迟 100ms 再 `setMqttConfig`，避免在等待期间触发依赖 `mqttConfig` 的 useEffect 重复连接/订阅。

2. **等待 8 秒**  
   - 使用 `setInterval` 每秒执行一次，到第 8 秒时 `clearInterval` 并 `resolve()`。  
   - 实际消息由 **useMQTTConnection** 建立连接并订阅后，在 **handleMQTTMessages** 中写入 **collectedMessagesRef.current**。  
   - 这里只负责「等时间到」，再从 **collectedMessagesRef.current** 读取当前已收集的消息，避免闭包拿到旧的 state。

3. **组装 data**  
   - **有消息**：`data = { _config: { name, topics, hostname, port, message: "已收集到实时数据" }, ...currentMessages }`。  
     - `currentMessages` 的 key 为主题名，value 为解析后的 JSON 或原始 payload。  
   - **无消息**：`data = { _config: { name, topics, hostname, port, message: "暂无实时数据...", debug_info: { isConnected, subscribedTopics, messageCount, error } } }`，便于排查连接/订阅是否正常。

### 4.3 WebSocket（约 808–828 行）

- 当前为占位实现：约 1.5 秒后 resolve 一段模拟数据（如 `realtime.speed`、`location`、`status`），10 秒未 resolve 则 reject「获取数据超时」。  
- 实际项目中可在此处接入真实 WebSocket 连接，收取若干条消息后拼成与 MQTT 类似结构的 `data` 再 resolve。

---

## 五、MQTT 消息如何被收集（与 fetchDataPreview 的配合）

消息不是在 fetchDataPreview 里接收的，而是由以下逻辑写入 **collectedMessagesRef**：

1. **useMQTTConnection(mqttConfig)**（约 624–645 行）  
   - 根据 `mqttConfig`（hostname、port、client_id、topics 等）创建连接对象；`mqttConnectionRef.current = mqttConnection`。

2. **连接与订阅**（约 953–1005 行）  
   - 一个 useEffect 在 `mqttConfig && visible` 且未连接时调用 `connection.connect()`。  
   - 另一个 useEffect 在连接成功后，根据 `mqttConfig.topics` 调用 `connection.subscribe(topic, qos)` 订阅，并 `connection.clearMessages()` 清空旧消息。

3. **handleMQTTMessages**（约 882–943 行）  
   - 当 `mqttConnection.messages` 变化时执行：遍历 `connection.messages`，对每条「已接收」且非 system 的消息，尝试 `JSON.parse(payload)`；  
   - 以 **topic** 为 key 写入 `newMessages[topic]`，再合并到 **collectedMessagesRef.current**，并 `setCollectedMessages` 更新 UI。

因此 **fetchDataPreview** 里「等 8 秒」是在等：连接建立、主题订阅、broker 推送的消息被 handleMQTTMessages 写入 **collectedMessagesRef.current**，最后在 8 秒结束时被读出来拼进 **data**。

---

## 六、数据结构与展示

- **setPreviewData(data)** 后，左侧「数据结构」使用 **buildDataTree(previewData)**（约 650–686 行）将 `previewData` 转为 Ant Design Tree 的 `treeData`。
- **buildDataTree**：递归遍历对象 key，为每个 key 生成节点（标题、path、value、type）；若 value 为对象则递归生成 children。  
- 用户点击树节点时 **handleTreeSelect** 会取节点的 `path` 写入 **selectedPath**；用户点击「选择此路径」后，**handleConfirmPath** 调用 **onPathSelect(selectedPath)**，将路径回填到 IoT 绑定表单的「数据源路径」输入框。

---

## 七、相关文件与行号速查

| 内容           | 文件                          | 位置/说明                    |
|----------------|-------------------------------|------------------------------|
| 数据预览弹窗   | DataPathHelper.tsx            | DataPreviewModal 组件         |
| fetchDataPreview | DataPathHelper.tsx         | 约 691–839 行                 |
| buildDataTree  | DataPathHelper.tsx            | 约 650–686 行                 |
| MQTT 消息收集  | DataPathHelper.tsx            | handleMQTTMessages 约 882–943 行；useEffect 订阅约 973–1005 行 |
| 预览入口按钮   | IoTBindingConfigModal.tsx     | 「数据源路径」旁云图标，约 1040–1050 行 |
| HTTP 配置接口  | 后端 / 前端 httpAPI           | getHTTPById(connectionId)     |
| MQTT 配置接口  | 后端 / 前端 mqttAPI           | getMQTTById(connectionId)      |

---

## 八、小结

- **数据预览** = 在 DataPreviewModal 中，根据当前 **protocol** 和 **connectionId**，通过 **fetchDataPreview** 拉取或等待一份数据，得到 **data** 后 **setPreviewData(data)**，再用 **buildDataTree(previewData)** 渲染左侧「数据结构」树，供用户选路径并回填到绑定配置的「数据源路径」。
- **HTTP**：拉配置 → fetch 一次 → 解析 JSON 或文本。  
- **MQTT**：拉配置（可选）→ 等 8 秒 → 从 collectedMessagesRef 取已收集消息 → 拼 _config + 消息或 _config + debug_info。  
- **WebSocket**：当前为占位模拟数据，可按需改为真实 WebSocket 收数后组合同样结构。
