# 查询实例说明

本文档说明「查询实例」的两个接口：平铺列表和树形结构，以及它们的区别、用途和返回格式。

---

## 一、流程概览

### 1. 查询实例列表（平铺）

1. 前端调用 **GET /scenes/{scene_id}/instances**，传入场景 uid。
2. 后端查场景，不存在则 404。
3. 获取场景的根实例，没有则返回空列表。
4. **递归平铺所有实例**：从根节点开始，递归遍历所有子节点，将非根实例加入结果列表。
5. 返回平铺的实例列表（排除根节点）。

### 2. 查询实例树（树形结构）

1. 前端调用 **GET /scenes/{scene_id}/instance-tree**，传入场景 uid。
2. 后端查场景，不存在则 404。
3. 获取场景的根实例，没有则返回空字典。
4. **递归构建树形结构**：从根节点开始，递归构建包含子节点的树形结构。
5. 返回完整的实例树（包含根节点）。

---

## 二、接口与路由

### 1. 查询实例列表（平铺）

**路径**：`GET /scenes/{scene_id}/instances`  
**文件**：`app/routers/scene.py`（约 223–247 行）

| 项目 | 说明 |
|------|------|
| 路径参数 | **scene_id**：场景的 uid |
| 请求体 | 无 |
| 依赖 | `get_current_active_user`（需登录） |
| 返回值 | `List[dict]`（实例列表，平铺结构） |

### 2. 查询实例树（树形结构）

**路径**：`GET /scenes/{scene_id}/instance-tree`  
**文件**：`app/routers/scene.py`（约 249–269 行）

| 项目 | 说明 |
|------|------|
| 路径参数 | **scene_id**：场景的 uid |
| 请求体 | 无 |
| 依赖 | `get_current_active_user`（需登录） |
| 返回值 | `dict`（树形结构，包含根节点） |

---

## 三、路由内逻辑（逐行）

### 1. 查询实例列表（scene.py 225–247）

```python
# 1. 查场景，不存在则 404
scene = Scene.nodes.get_or_none(uid=scene_id)
if not scene:
    raise HTTPException(404, "场景不存在")

# 2. 获取场景的根实例，没有则返回空列表
root = scene.root.single()
if not root:
    return []

# 3. 递归平铺所有实例（排除根节点）
result = []
def flatten(inst):
    # 跳过根节点
    if inst != root:
        result.append({
            "uid": inst.uid,
            "name": inst.name,
            "transform": inst.transform,
            "materials": inst.materials,
            "asset_id": inst.asset_id,
            "asset_type": inst.asset_type
        })
    # 递归处理所有子节点
    for child in inst.children:
        flatten(child)
flatten(root)

# 4. 返回平铺列表
return result
```

**关键点**：
- **递归遍历**：`flatten(inst)` 递归遍历实例树。
- **排除根节点**：`if inst != root:` 跳过根节点，只返回实际创建的实例。
- **返回字段**：uid, name, transform, materials, asset_id, asset_type（不包含 properties、iot_binds 等）。

---

### 2. 查询实例树（scene.py 251–269）

```python
# 1. 查场景，不存在则 404
scene = Scene.nodes.get_or_none(uid=scene_id)
if not scene:
    raise HTTPException(404, "场景不存在")

# 2. 获取场景的根实例，没有则返回空字典
root = scene.root.single()
if not root:
    return {}

# 3. 递归构建树形结构（包含根节点）
def build_tree(inst):
    return {
        "uid": inst.uid,
        "name": inst.name,
        "asset_id": inst.asset_id,
        "asset_type": inst.asset_type,
        "properties": inst.properties,
        "iot_binds": inst.iot_binds,
        "video_binds": inst.video_binds,
        "file_binds": inst.file_binds,
        "children": [build_tree(child) for child in inst.children]
    }

# 4. 返回根节点的树形结构
return build_tree(root)
```

**关键点**：
- **递归构建**：`build_tree(inst)` 递归构建子树。
- **包含根节点**：从 `build_tree(root)` 开始，包含根节点。
- **返回字段**：uid, name, asset_id, asset_type, properties, iot_binds, video_binds, file_binds, children（不包含 transform、materials）。
- **children 字段**：通过列表推导递归构建（`[build_tree(child) for child in inst.children]`）。

---

## 四、两个接口的对比

| 项目 | list_instances（平铺列表） | get_instance_tree（树形结构） |
|------|---------------------------|-------------------------------|
| 路径 | GET /scenes/{scene_id}/instances | GET /scenes/{scene_id}/instance-tree |
| 返回类型 | `List[dict]` | `dict` |
| 结构 | 平铺（一维数组） | 树形（嵌套对象） |
| 根节点 | ❌ 排除根节点 | ✅ 包含根节点 |
| 返回字段 | uid, name, transform, materials, asset_id, asset_type | uid, name, asset_id, asset_type, properties, iot_binds, video_binds, file_binds, children |
| 包含 transform | ✅ 是 | ❌ 否 |
| 包含 materials | ✅ 是 | ❌ 否 |
| 包含 properties | ❌ 否 | ✅ 是 |
| 包含 iot_binds | ❌ 否 | ✅ 是 |
| 包含 video_binds | ❌ 否 | ✅ 是 |
| 包含 file_binds | ❌ 否 | ✅ 是 |
| 包含 children | ❌ 否（平铺结构） | ✅ 是（树形结构） |
| 用途 | 快速获取所有实例的列表 | 获取完整的实例树结构 |

---

## 五、关键点说明

### 1. 递归遍历

两个接口都使用递归：
- **list_instances**：`flatten(inst)` 递归平铺所有实例。
- **get_instance_tree**：`build_tree(inst)` 递归构建树形结构。

**递归原理**：
- 从根节点开始，遍历所有子节点。
- 对每个子节点，继续遍历其子节点。
- 直到没有子节点为止。

---

### 2. 根节点的处理

**list_instances**：
- **排除根节点**：`if inst != root:` 跳过根节点。
- **原因**：根节点是系统自动创建的（"Root of xxx"），不是用户创建的实例。

**get_instance_tree**：
- **包含根节点**：从 `build_tree(root)` 开始，包含根节点。
- **原因**：树形结构需要完整的层级关系，根节点是树的起点。

---

### 3. 返回字段的差异

**list_instances**：
- ✅ 包含：uid, name, transform, materials, asset_id, asset_type
- ❌ 不包含：properties, iot_binds, video_binds, file_binds
- **用途**：快速获取实例的基本信息和变换信息。

**get_instance_tree**：
- ✅ 包含：uid, name, asset_id, asset_type, properties, iot_binds, video_binds, file_binds, children
- ❌ 不包含：transform, materials
- **用途**：获取完整的实例树结构和绑定信息。

---

### 4. 性能考虑

**list_instances**：
- **优点**：字段少，返回速度快。
- **缺点**：不包含完整的实例信息（如 properties、iot_binds）。
- **适用场景**：快速获取实例列表，用于列表展示、搜索等。

**get_instance_tree**：
- **优点**：包含完整的实例信息和树形结构。
- **缺点**：字段多，递归构建可能较慢。
- **适用场景**：获取完整的实例树结构，用于树形展示、编辑等。

---

## 六、数据来源

| 数据 | 存储位置 | 说明 |
|------|----------|------|
| Instance 节点属性 | **Neo4j** | 通过 `scene.root.single()` 和 `inst.children` 从 Neo4j 查询 |
| 实例树结构 | **Neo4j** | 通过 PARENT_OF 关系构建树形结构 |

**查询流程**：
1. 通过 `scene.root.single()` 获取场景的根实例。
2. 通过 `inst.children` 获取子节点（neomodel 自动查询 PARENT_OF 关系）。
3. 递归遍历所有子节点，构建列表或树形结构。

---

## 七、示例返回

### list_instances 返回示例

```json
[
  {
    "uid": "instance1",
    "name": "实例1",
    "transform": {
      "location": [1, 0, 0],
      "rotation": [0, 0, 0],
      "scale": [1, 1, 1]
    },
    "materials": [],
    "asset_id": "asset1",
    "asset_type": "model"
  },
  {
    "uid": "instance2",
    "name": "实例2",
    "transform": {
      "location": [2, 0, 0],
      "rotation": [0, 0, 0],
      "scale": [1, 1, 1]
    },
    "materials": [],
    "asset_id": "asset2",
    "asset_type": "model"
  }
]
```

**特点**：
- 平铺结构（一维数组）。
- 排除根节点。
- 包含 transform、materials。

---

### get_instance_tree 返回示例

```json
{
  "uid": "root-instance",
  "name": "Root of 场景1",
  "asset_id": null,
  "asset_type": "model",
  "properties": {},
  "iot_binds": [],
  "video_binds": [],
  "file_binds": [],
  "children": [
    {
      "uid": "instance1",
      "name": "实例1",
      "asset_id": "asset1",
      "asset_type": "model",
      "properties": {},
      "iot_binds": [],
      "video_binds": [],
      "file_binds": [],
      "children": [
        {
          "uid": "instance1-1",
          "name": "实例1-1",
          "asset_id": "asset1-1",
          "asset_type": "model",
          "properties": {},
          "iot_binds": [],
          "video_binds": [],
          "file_binds": [],
          "children": []
        }
      ]
    },
    {
      "uid": "instance2",
      "name": "实例2",
      "asset_id": "asset2",
      "asset_type": "model",
      "properties": {},
      "iot_binds": [],
      "video_binds": [],
      "file_binds": [],
      "children": []
    }
  ]
}
```

**特点**：
- 树形结构（嵌套对象）。
- 包含根节点。
- 包含 properties、iot_binds、video_binds、file_binds。
- children 字段递归包含所有子节点。

---

## 八、相关文件

| 说明 | 文件路径 |
|------|----------|
| 查询实例列表接口 | `app/routers/scene.py`（list_instances） |
| 查询实例树接口 | `app/routers/scene.py`（get_instance_tree） |
| Scene 模型 | `app/models/scene.py`（Scene 类） |
| Instance 模型 | `app/models/scene.py`（Instance 类） |

---

## 九、小结

- **查询实例列表**：GET /scenes/{scene_id}/instances，返回平铺的实例列表（排除根节点），包含 transform、materials，适合快速获取实例列表。
- **查询实例树**：GET /scenes/{scene_id}/instance-tree，返回树形结构（包含根节点），包含 properties、iot_binds、video_binds、file_binds，适合获取完整的实例树结构。
- **递归遍历**：两个接口都使用递归遍历实例树，list_instances 平铺，get_instance_tree 构建树形结构。
- **根节点处理**：list_instances 排除根节点，get_instance_tree 包含根节点。
- **返回字段差异**：list_instances 包含 transform、materials，get_instance_tree 包含 properties、iot_binds、video_binds、file_binds、children。
