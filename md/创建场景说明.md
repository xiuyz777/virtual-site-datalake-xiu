# 创建场景说明

本文档说明系统中「创建场景」的接口、数据流、以及保存到 Neo4j 的逻辑。

---

## 一、流程概览

1. 前端调用 **POST /scenes**，请求体为场景名称等（SceneCreate）。
2. 后端 **create_scene** 创建 Scene 对象，设置 name、owner、origin 等，调用 **scene.save()**。
3. **Scene.save()** 先保存 Scene 节点（生成 sceneId），若无根节点则创建根 Instance 并建立 **Scene -[:ROOT]-> 根 Instance** 关系。
4. 返回 **scene.uid**（sceneId）给前端。

---

## 二、接口与路由

**路径**：POST `/scenes`  
**文件**：`app/routers/scene.py`（约 21–36 行）

| 项目 | 说明 |
|------|------|
| 路由装饰器 | `@router.post("/scenes", response_model=dict)` |
| 请求体模型 | **SceneCreate**（name 必填，origin、chart_binds、tiles_binding 可选） |
| 依赖 | `get_current_active_user`（需登录） |
| 返回值 | `{"uid": scene.uid, "name": scene.name, "owner": scene.owner, "origin": scene.origin, "chart_binds": ..., "tiles_binding": ...}` |

---

## 三、路由内逻辑（逐行）

```python
# 1. 创建 Scene 对象（内存中，未写入 Neo4j）
scene = Scene(name=data.name, owner=str(current_user.id))

# 2. 设置可选字段
if data.origin:
    scene.origin = data.origin
else:
    scene.origin = {"longitude": 113, "latitude": 23, "height": 50}
if data.chart_binds:
    scene.chart_binds = data.chart_binds
if data.tiles_binding:
    scene.tiles_binding = data.tiles_binding

# 3. 保存到 Neo4j（内部会创建根 Instance 并建 ROOT 关系）
scene.save()

# 4. 返回场景 uid 等信息
return {"uid": scene.uid, "name": scene.name, ...}
```

- **scene.uid**：在 **scene.save()** 执行时由 neomodel 的 **UniqueIdProperty** 自动生成，即 **sceneId**。

---

## 四、Scene 模型的 save()（创建根实例与 ROOT 关系）

**文件**：`app/models/scene.py`（约 80–87 行）
    class Scene(StructuredNode):
    uid          = UniqueIdProperty()
    name         = StringProperty()
    owner        = StringProperty()  # 新增，记录用户id
    preview_image = StringProperty()  # 新增，minio预览图链接
    created_at   = DateTimeProperty(default_now=True)
    updated_at   = DateTimeProperty(default=lambda: datetime.utcnow())
    origin       = JSONProperty(default=lambda: {"longitude": 0.0, "latitude": 0.0, "height": 0.0})  # 新增，场景原点
    chart_binds  = JSONProperty(default=list)               # 新增, 绑定的图表ID列表
    tiles_binding = JSONProperty(default=dict) 

    Neo4j 的节点模型，每个场景在图里是一个节点，属性会存成节点属性。
    uid = UniqueIdProperty()
    唯一 ID，neomodel 会生成（如 UUID），用来在接口里标识场景（如 /scenes/{uid}）。
    name = StringProperty()
    场景名称，用户填的。
    owner = StringProperty()
    创建者 ID（一般是 current_user.id 转成字符串），用于权限和「我的场景」筛选。
    preview_image = StringProperty()
    预览图地址，通常是 MinIO 的 URL 或路径。
    created_at / updated_at
    创建、更新时间；default_now=True 表示创建时自动填当前时间。
    origin = JSONProperty(default=...)
    场景原点，经纬度 + 高程，例如 {"longitude": 113, "latitude": 23, "height": 50}，用于 Cesium 等定位。
    chart_binds
    绑定的图表 ID 列表（如 GoView 图表），JSON 数组。
    tiles_binding
    绑定的 WMTS 底图，例如 {"wmts_id": "xxx", "enabled": true}，JSON 对象。 


=====================================================================================================
    # 根节点（单向；场景只有一个 ROOT）
    root         = RelationshipTo('Instance', 'ROOT', cardinality=One)
        RelationshipTo：表示「从当前节点（Scene）指向别的节点」的关系。
        'Instance'：指向的节点类型是 Instance。
        'ROOT'：这条边的名字叫 ROOT（在 Neo4j 里就是关系类型）。
         cardinality=One：一个 Scene 只能连一个 Instance（一个场景只有一个根）。
          (Scene) --ROOT--> (Instance)
            场景               根实例
        含义：每个场景有一个「根实例」，这个根实例是场景里实例树的根，其它实例都挂在它下面。
        所以这句的意思是：Scene 通过 ROOT 关系指向唯一一个 Instance（根节点）。    
    # 场景层级关系
    parent_scene = RelationshipFrom('Scene', 'SCENE_PARENT_OF', model=SceneParentRel)
    child_scenes = RelationshipTo('Scene', 'SCENE_PARENT_OF', model=SceneParentRel)
        root = RelationshipTo('Instance', 'ROOT', cardinality=One)
        这里用的是同一种关系类型 SCENE_PARENT_OF，只是方向不同：
        RelationshipTo：从「我」指出去的那一侧（我 → 别人）
        RelationshipFrom：从别人指回「我」的那一侧（别人 → 我）
        所以：
        child_scenes = RelationshipTo('Scene', 'SCENE_PARENT_OF', ...)
        「我」通过 SCENE_PARENT_OF 指向别的 Scene → 那些 Scene 是我的子场景。
        parent_scene = RelationshipFrom('Scene', 'SCENE_PARENT_OF', ...)
        别的 Scene 通过 SCENE_PARENT_OF 指向「我」→ 那个 Scene 是我的父场景。
        在图里（A 是父、B 是子）：
        (Scene A) --SCENE_PARENT_OF--> (Scene B)
        父场景                         子场景
        对 Scene B 来说：parent_scene 能取到 A，child_scenes 可以再指向更下一层。
        对 Scene A 来说：child_scenes 能取到 B（以及其它子场景）。
        model=SceneParentRel 表示这条边上还可以带属性（比如时间戳），用 SceneParentRel 这个类来定义。
        74-75 行：场景和「根实例」的关系：一个场景有一个 ROOT 指向一个 Instance。
        77-79 行：场景和场景的父子关系：用 SCENE_PARENT_OF 表示谁是谁的父场景、谁是谁的子场景；RelationshipTo 表示「我→子」，RelationshipFrom 表示「父→我」。
scenes = RelationshipFrom(Scene, 'HAS_INSTANCE')
        表示「从 Scene 指向当前 Instance 的 HAS_INSTANCE 关系」。
        图里：(Scene) -[:HAS_INSTANCE]-> (Instance)
        在创建实例时有一行：inst.scenes.connect(scene)，就是建这一条关系。
        这样一个实例可以被多个场景复用（多个 Scene -[:HAS_INSTANCE]-> 同一个 Instance），而 inst.scenes 可以看到它属于哪些场景。   
             
        一句话总结
        parent：我的父节点（父实例）。
        children：我有哪些子节点（子实例）。
        scenes：哪些场景包含了我这个实例。
=====================================================================================================


Scene 继承自 **StructuredNode**（neomodel），并重写了 **save()**：

```python
def save(self):
    # 1. 先保存 Scene 节点到 Neo4j（生成 Scene 的 uid）
    super().save()   # 调用 StructuredNode.save()

    # 2. 若还没有根节点，则创建根 Instance 并建立关系
    if not self.root:
        root_instance = Instance(name=f"Root of {self.name}").save()  # 创建并保存根实例（生成根实例的 uid）
        self.root.connect(root_instance)   # 建立 Scene -[:ROOT]-> 根 Instance 关系
    return self
```

| 步骤 | 代码 | 作用 |
|------|------|------|
| 1 | `super().save()` | 父类 **StructuredNode** 的 save，将 Scene 节点写入 Neo4j，生成 **Scene.uid**（第 1 个 uid） |
| 2 | `if not self.root:` | 判断是否尚未有根节点（首次创建场景时为真） |
| 3 | `Instance(...).save()` | 创建根 Instance 节点并保存，生成 **根 Instance.uid**（第 2 个 uid） |
| 4 | `self.root.connect(root_instance)` | 在 Neo4j 中建立 **Scene -[:ROOT]-> 根 Instance** 关系 |

- **父类**：`Scene` 的父类是 **StructuredNode**（neomodel），故 `super().save()` 即 **StructuredNode.save()**。

---

## 五、生成的两个 uid

| 序号 | 来源 | 含义 |
|------|------|------|
| 第 1 个 uid | Scene 节点 | **sceneId**，场景唯一标识，返回给前端用于编辑/预览等 |
| 第 2 个 uid | 根 Instance 节点 | 根实例唯一标识，用于挂载实例树（PARENT_OF） |

两者均在 **scene.save()** 执行过程中生成并写入 Neo4j。

---

## 六、数据保存位置

| 内容 | 存储位置 |
|------|----------|
| Scene 节点（uid、name、owner、origin、chart_binds、tiles_binding 等） | **Neo4j**，标签 **Scene** |
| 根 Instance 节点（uid、name 等，transform 为模型默认值） | **Neo4j**，标签 **Instance** |
| Scene 与根实例的关系 | **Neo4j**，关系类型 **ROOT**（Scene -[:ROOT]-> 根 Instance） |

---

## 七、相关文件

| 说明 | 文件路径 |
|------|----------|
| 创建场景接口 | `app/routers/scene.py`（create_scene） |
| 请求体模型 SceneCreate | `app/models/scene.py` |
| Scene 模型与 save() | `app/models/scene.py`（Scene 类） |
| Instance 模型 | `app/models/scene.py`（Instance 类） |

---

## 八、小结

- **创建场景**：前端 POST /scenes → 后端 **create_scene** 构建 Scene 并 **scene.save()**。
- **scene.save()**：先 **super().save()** 保存 Scene 节点（生成 sceneId），再若无根则创建根 Instance 并 **self.root.connect(root_instance)** 建立 ROOT 关系。
- **两个 uid**：Scene.uid（sceneId）与根 Instance.uid，均在本次 save 中生成并写入 Neo4j。
==============================================================================================
下面用更通俗的说法，把「创建场景」从头到尾讲一遍，方便不熟悉系统的人理解：

背景：这是个什么系统
这是一个数字孪生/3D 场景平台：用户可以在里面创建 3D 场景，并在场景里摆放模型实例。
场景和实例的数据都存在一个叫 Neo4j 的图数据库里：节点表示「场景」「实例」这类实体，边表示它们之间的从属、父子等关系。

什么是「场景」和「根实例」
场景（Scene）：一个独立的 3D 场景，比如「某车间」「某园区」，是顶层容器。
根实例（Root Instance）：每个场景内部的「根节点」，不显示具体模型，主要作用是作为实例树的根。
用户后面创建的模型实例（如设备、建筑）都会挂在根实例下面，形成一棵树。
可以理解为：场景 = 文件夹，根实例 = 该文件夹里的「根目录」。
用户在前端做了什么
用户在界面上填写场景名称（必填）和可选的场景原点（经纬度、高程）、图表绑定、瓦片底图等，点击「创建场景」。
前端会向后端发送一个 POST /scenes 请求，把上述信息放到请求体里。

后端收到请求后做了什么
校验用户身份
后端先检查用户是否已登录；未登录就返回 401，不允许创建。

根据请求体创建 Scene 对象
用请求里的 name 和当前用户的 id（作为 owner）在内存里新建一个 Scene 对象，并设置 origin、chart_binds、tiles_binding 等。此时还未写入数据库。

执行 scene.save()，把 Scene 真正写进 Neo4j
这一步会依次做几件事：

保存 Scene 节点：调用父类的 save()，把 Scene 作为一个 Neo4j 节点写入数据库。
由 neomodel 的 UniqueIdProperty 自动生成一个 UUID 作为 scene.uid（如 a1b2c3d4-e5f6-7890-abcd-ef1234567890），并写入节点属性。
判断是否需要创建根实例：检查这个 Scene 是否已经有一条指向根 Instance 的 ROOT 关系。
若是首次创建，则还没有根实例，需要新建。
创建根 Instance 并建立关系：
新建一个 Instance，名字为 "Root of {场景名}"（如 "Root of 车间1"）。
调用该 Instance 的 save()，把它写入 Neo4j，同样由 UniqueIdProperty 自动生成根 Instance 的 uid。
调用 self.root.connect(root_instance)，在 Neo4j 里建立一条从当前 Scene 指向该根 Instance 的有向边，关系类型为 ROOT。
这样在 Neo4j 里就有：(Scene) -[:ROOT]-> (根 Instance)。
把结果返回给前端
返回 scene.uid、name、owner、origin、chart_binds、tiles_binding 等字段，供前端用于跳转到编辑页、展示场景列表等。

Neo4j 里最终长什么样
创建完成后，Neo4j 里会有：

一个 Scene 节点：包含 uid、name、owner、origin 等；
一个 Instance 节点：即根实例，包含 uid、name（如 "Root of 车间1"）等；
一条 ROOT 关系：从 Scene 指向根 Instance。
以后用户在该场景里创建新实例时，这些实例会通过 PARENT_OF 关系挂在根实例下面，形成一棵树。

一句话总结
创建场景时，后端会先保存 Scene 节点，再自动创建一个名为「Root of {场景名}」的根 Instance，并用 ROOT 关系把它和 Scene 连在一起，两个 uid 都由 neomodel 在 save() 时自动生成。
